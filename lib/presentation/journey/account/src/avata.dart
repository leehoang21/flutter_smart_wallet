import 'package:flutter/material.dart';

class Avata extends StatelessWidget {
  Avata({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/icons/avata_default.png');
  }
}
